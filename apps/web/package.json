{
  "name": "@ai-network-check/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "@ai-network-check/core": "0.0.0",
    "@ai-network-check/web-reporting": "0.0.0",
    "@ai-network-check/web-workflow": "0.0.0",
    "@ai-network-check/websocket-benchmark": "0.0.0"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.1.5"
  }
}
